services:
  - type: web
    name: netease-checker-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: netease-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true

  - type: web
    name: netease-checker-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./frontend/dist
    envVars:
      - key: VITE_API_URL
        value: https://netease-checker-backend.onrender.com

  - type: postgresql
    name: netease-db
    plan: starter